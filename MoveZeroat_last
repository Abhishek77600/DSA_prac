class Solution:
    def moveZeroes(self, nums):
        temp=[]
        for i in range(len(nums)):
            if nums[i]!= 0:
                temp.append(nums[i])
        for i in range(len(nums)):
            nums[i]=temp[i]
        
    
        for i in range(len(temp),len(nums)):
            nums[i]=0
