def bubble_sort(arr):
    n = len(arr)
    for i in range(n-1):
        for j in range(n-i-1):
            if arr[j]>arr[j+1]:
                arr[j],arr[j+1]=arr[j+1],arr[j]
    return arr
        
arr = [5,3, 2, 4, 6,5, 1, 3,1,29,90]
print("Original:", arr)
bubble_sort(arr)
print("Sorted:", arr)
